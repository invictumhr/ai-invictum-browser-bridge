# Relay deployment

The remote relay is intentionally deferred until the local bridge is complete and stable. There is currently nothing to deploy.

Phase 8 will use outbound WSS from the desktop, short-lived tokens, device key pairs, revocation, one active control session by default, and end-to-end encryption where feasible. The relay must not retain browser content or require inbound router ports.
